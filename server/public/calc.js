$(document).ready(init);

let operator = '';

const calculate = [];
function init() {
  console.log('Where are you running!?!');

  return calcNum();
}

function calcNum() {
  let inputOne = $('firstNum').val();
  let inputTwo = $('secondNum').val();
}

// Accept number inputs

// calculate the numbers

// clear, clear history?
